export const DATA_BASE_CONFIGURATION = {
    pgConnectionString: "postgresql://postgres:postgres@localhost:5432/wtworks"  // process.env
      // .PG_CONNECTION_STRING as string,
  };

  export const JWT_SECRET = {
    secret: "some_secrets"  //process.env.JWT_SECRET
  }